 /*
if...else ka short form
👉 Jab decision sirf 2 options ka ho

syntax:

condition ? true_wala_part : false_wala_part

Isko aise padho:

“Agar condition true hai → ye karo
warna → wo karo”
 */
  const age = 30;

  const result = age>=20 ? " can drive" : "can not drive";

  console.log(result);// can drive

  console.log(`rahul ${age>=20 ? "can drive" : "can not drive"}`);//rahul can drive
  

  // 

  const Age = 25;
   
  Age>= 26 ? console.log(' can drink  wine'):console.log(' can not drink wine');//  can not drink wine
  
// playwright

const status = "isChecked"
  ? "Checkbox checked hai ☑️"
  : "Checkbox checked nahi hai ⬜";

console.log(status);// Checkbox checked hai ☑️
  
  
  